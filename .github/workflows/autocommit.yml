name: Auto commit

on:

  push:
    branches:
      - master
      
  schedule:
  - cron: "*/10 * * * *"

jobs:
  auto_commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2      
        with:
         persist-credentials: false
         fetch-depth: 0

      - name: Modify last update
        run: |
          d=`date '+%Y-%m-%dT%H:%M:%SZ'`
          echo $d > LAST_UPDATED
          
      - name: Commit changes
        run: |
          git config --local user.email "mike@mikeowino.com"
          git config --local user.name "mikeyhodl"
          git add -A
          
          arr[0]="reimagined-journey"
          arr[1]="reimagined-journey"
          arr[2]="reimagined-journey"
          arr[3]="reimagined-journey"
          arr[4]="reimagined-journey"
          arr[5]="reimagined-journey"
          arr[6]="reimagined-journey"
          arr[7]="reimagined-journey"
          
          rand=$[$RANDOM % ${#arr[@]}]
          
          git commit -m "${arr[$rand]}"
          
      - name: GitHub Push
        uses: ad-m/github-push-action@v0.5.0
        with:
          force: true
          directory: "."
          github_token: ${{ secrets.GITHUB_TOKEN }}
